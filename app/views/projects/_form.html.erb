<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>
      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group row">
    <%= f.label :title, class: "col-sm-4 form-control-label" %>
    <div class="col-sm-8">
      <%= f.text_field :title, class: "form-control" %>
    </div>
  </div>
  <div class="form-group row">
    <%= f.label :description, class: "col-sm-4 form-control-label" %>
    <div class="col-sm-8">
      <%= f.text_area :description, class: "form-control" %>
    </div>
  </div>
  <div class="form-group row">
    <%= f.label :project_link, class: "col-sm-4 form-control-label" %>
    <div class="col-sm-8">
      <%= f.text_field :project_link, class: "form-control" %>
    </div>
  </div>
  <div class="form-group row">
    <%= f.label :website_link, class: "col-sm-4 form-control-label" %>
    <div class="col-sm-8">
      <%= f.text_field :website_link, class: "form-control" %>
    </div>
  </div>
  <div class="actions" id="newPeojectBtn">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
